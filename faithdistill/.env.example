# 复制这个文件为 .env 并按需修改（install.sh 会自动帮你做这一步）

# 指向和 obsidian-ai-dashboard 插件同一个 Obsidian Vault 的绝对路径
OBSIDIAN_VAULT=/path/to/your/Obsidian/Vault

# 本地 Ollama（默认）或任意 OpenAI 兼容接口
LLM_BASE_URL=http://localhost:11434/v1
LLM_API_KEY=ollama
LLM_MODEL=qwen3:8b
